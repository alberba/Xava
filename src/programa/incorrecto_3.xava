funcion entero suma(ent a, ent b);

principal {
    ent res := 0;
    ent [2] valores;
    valores[0] := 0;
    valores[1] := 1;

    ## ERROR: Funcion no declarada
    res = suma_2(valores[0], 5);
    imprimir(res);

    ## ERROR: Índice fuera de rango
    res = suma(valores[2], 5);
    imprimir(res);
}

funcion entero suma(ent a, ent b) {
    devuelve a + b;
}