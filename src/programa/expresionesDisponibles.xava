funcion ent probar(ent contador);

principal{
    ent contador := 5;
    imprimir(probar(contador));
}

funcion ent probar(ent contador){
    ent a := contador * 4;
    ent b := contador - 4;
    ent result := contador * 4;

    devuelve result;
}